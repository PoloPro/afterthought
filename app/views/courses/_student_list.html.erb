<% unless  students.empty? %>
  <% students.each do |student| %>
    <b><h4 class='student-list'><b><%= student.name %></b></h4>
    <p class='student-list'><%= student.email %></p>
    <br>
  <% end %>
<% else %>
  <p class='text-muted'>
    There are currently no students in this course
  </p>
<% end %>
